name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore .\InstantTranslate.sln

      - name: Build
        run: dotnet build .\InstantTranslate.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test .\InstantTranslate.sln --configuration Release --no-build
